# Novel Studio v0.1.0 Beta 发布验收记录

Version: 1.6 | Status: Review Complete | Date: 2026-07-05

## 范围

本次发布验收覆盖当前定义的 M0-M24。发布模式仍为本地 beta：

- 不 push。
- 不创建 tag。
- 不上传 artifact。
- 不访问真实模型 endpoint。
- 不要求真实签名凭证。

## 本地状态

- 分支：`main`
- 远程：`origin` -> `https://github.com/47ronintop/Novel-Studio.git`
- 当前本地提交包含 M16 Story Bible、M17 Installer and Release Channel、M18 Plugin System、beta 启动修复、项目启动 UX、M19 Beta UX 产品化打磨、M20 Search and Index UX、M21 Story Bible Editing UX、M22 Settings UX Completion、M23 Studio UX Completion 和 M24 工作流运行观测。
- `release/` 目录被 `.gitignore` 忽略，本地安装器产物不进入仓库。

## 当前产物

最新安装器记录在 `release/latest-installer.txt`：

```text
release/installer-20260705-095109/Novel-Studio-0.1.0-win-x64.exe
```

本次 `package:installer` 同时生成：

- Windows NSIS installer。
- Windows unpacked artifact。
- `latest.yml` 与 blockmap。
- `release/notes/novel-studio-v0.1.0-beta.md`。

## Release Notes

`docs/releases/v0.1.0-beta.md` 已覆盖：

- M17 安装器与发布通道。
- M18 插件系统边界。
- Beta 启动修复。
- Beta 启动 UX 与错误反馈。
- M19 Beta UX 产品化打磨。
- M20 Search and Index UX。
- M21 Story Bible Editing UX。
- M22 Settings UX Completion。
- M23 Studio UX Completion。
- M24 工作流运行观测。
- 当前 beta 包含 M0-M24。

`release:check` 会校验 release notes 包含 M17 与 M18 条目。M19-M24 属于 Post-M18 本地产品化打磨，当前通过本验收记录和 changelog 跟踪。

## 验证证据

本次 M24 验收已运行：

- `npm run format`
- `npm run typecheck`
- `npm run lint`
- `npm run test`：57 files / 208 tests
- `npm run test:contract`：41 tests
- `npm run test:e2e`：4 tests
- `npm audit`：0 vulnerabilities
- `npm run release:check`
- `npm run package:check`
- `npm run alpha:check`
- `npm run package:installer`
- `npm run package:artifact-check`
- Packaged smoke：启动 `release/installer-20260705-095109/win-unpacked/Novel Studio.exe`，确认 packaged executable 可启动并保持进程运行。

## 非阻塞风险

- 当前 beta artifact 仍是 unsigned local beta；正式对外分发前需要证书签名策略。
- 当前 release channel 是本地 manifest；尚未接入 hosted update publishing。
- electron-builder 输出 `package.json` 缺少 `author` 的提示；不阻塞本地 beta，但正式发布前应补齐发布者元数据。
- 尚未执行新安装包的人工安装/卸载 smoke；自动化已覆盖打包、schema、artifact secret scan、Electron E2E、无源码 fixture 的安装版启动场景、项目错误反馈、左侧活动栏切换和 packaged executable smoke。
- 当前存在多个本地提交尚未 push；任何 push 必须由用户另行确认。

## 下一步

- 人工运行最新 NSIS installer，确认安装、启动、创建项目和卸载路径。
- 若继续产品化，应进入 M25 工作流运行历史。
- 用户确认后再决定是否 push 当前本地提交。

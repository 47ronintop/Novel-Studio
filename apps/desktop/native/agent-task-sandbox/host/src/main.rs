// Agent Task Sandbox Host — stub main
// Prints "unavailable" to stderr and exits 1.
// Real implementation uses Windows AppContainer/LowBox via WinAPI.
fn main() {
    eprintln!("unavailable");
    std::process::exit(1);
}

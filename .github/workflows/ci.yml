name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  quality-gates:
    name: Quality Gates
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20.20.0"
          cache: npm

      - name: Setup locked Rust toolchain
        uses: dtolnay/rust-toolchain@1.79.0

      - name: Install locked cargo-deny
        run: cargo install --locked cargo-deny --version 0.15.4

      - name: Install dependencies
        run: npm ci

      - name: Changed-file formatting (full-format debt retained locally)
        env:
          FORMAT_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          FORMAT_HEAD_SHA: ${{ github.sha }}
        run: npm run format:changed

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Unit and integration tests
        run: npm run test

      - name: Electron E2E smoke
        run: npm run test:e2e:built

      - name: Package preflight
        run: npm run package:verify

      - name: Release channel check
        run: npm run release:check

      - name: Alpha gate
        run: npm run alpha:verify

      - name: Dependency audit
        run: npm audit --omit=dev --audit-level=high

      - name: Native sandbox supply-chain audit
        run: npm run agent-sandbox:audit

      - name: Build locked Windows x64 sandbox host and probe
        run: npm run agent-sandbox:build

      - name: Build locked Windows x64 no-follow file operation host
        run: npm run agent-file-operations:build

      - name: Run sandbox host/probe qualification vectors
        run: npm run agent-sandbox:qualify

      - name: Run no-follow file operation qualification vectors
        run: npm run agent-file-operations:qualify

      - name: Package sandbox build inputs
        env:
          NOVEL_STUDIO_AGENT_SANDBOX_DIR: release/agent-task-sandbox
          NOVEL_STUDIO_AGENT_FILE_OPERATIONS_DIR: release/agent-file-operations
        run: npm run package:dir:built
